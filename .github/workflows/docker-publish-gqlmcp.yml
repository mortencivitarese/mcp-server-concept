name: Build and Push GqlMcp Docker Image to ACR

on:
  push:
    paths:
      - 'MCPServers/GqlMcp/**'
      - '.github/workflows/docker-publish-gqlmcp.yml'
      - 'Infrastructure/containerApp-GqlMcp.dev.bicepparam'
    branches:
      - main
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish-dev:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    uses: ./.github/workflows/docker-publish-template.yml
    with:
      mcp_server: 'GqlMcp'
      image_name: 'gqlmcp'
      docker_context: 'MCPServers'
      environment: dev
    secrets: inherit

  deploy-dev:
    needs: publish-dev
    if: needs.publish-dev.result == 'success'
    concurrency:
      group: azure-deploy-dev
      cancel-in-progress: false
    uses: ./.github/workflows/docker-deploy-containerapp-template.yml
    with:
      parameter_file_name: containerApp-GqlMcp.dev.bicepparam
      environment: dev
    secrets: inherit

  publish-prod:
    if: github.event_name == 'release' && startsWith(github.ref, 'refs/tags/v') && startsWith(github.event.release.name, 'GqlMcp')
    uses: ./.github/workflows/docker-publish-template.yml
    with:
      mcp_server: 'GqlMcp'
      image_name: 'gqlmcp'
      docker_context: 'MCPServers'
      environment: prod
    secrets: inherit

  deploy-prod:
    needs: publish-prod
    if: needs.publish-prod.result == 'success'
    concurrency:
      group: azure-deploy-prod
      cancel-in-progress: false
    uses: ./.github/workflows/docker-deploy-containerapp-template.yml
    with:
      parameter_file_name: containerApp-GqlMcp.prod.bicepparam
      environment: prod
      image_tag: ${{ needs.publish-prod.outputs.image_tag }}
    secrets: inherit
